<?php
// If uninstall.php is not called by WordPress, die.
if ( ! defined( 'WP_UNINSTALL_PLUGIN' ) ) {
	die;
}

/**
 * When the plugin is uninstalled, you'll want to clear out any plugin options
 * and/or settings specific to the plugin, and/or other database entities such
 * as tables.
 */
